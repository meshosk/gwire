# part editor

- this editor has its own page  at "/part-editor"
- part editor has two column layout:
	- left column has 
		- button to add points and states 
		- after edit state ther will next column, where can be added name to state and create point groups
	- Right state show scheme of points and user can move them using drag. Graphic is generated using SVG.