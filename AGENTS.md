# AGENTS.md

## Project Context
- Tech Stack: Node.js, TypeScript

## Development Commands
- Node is running using docker as: 'docker run --rm -it -v ${PWD}:/mnt/app -p 80:80 -w "/mnt/app/" node:latest bash' and then adding node command from package.json or npm or npx command
- Before completing task try build, it must be successful

## Do not do
- do not use static imports