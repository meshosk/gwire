# AGENTS.md

## Project Context
- Tech Stack: Node.js, TypeScript

## Development Commands
- Node is running using docker as: 'docker run --rm -it -v ${PWD}:/mnt/app -w "/mnt/app/" node:latest' and then adding node command from package.json or npm or npx command.
